<p><%= @order.name %>さんが晩メシを食べようと言っています。</p>
<p><%= @order.name %>さんは<%= @order.comment %>と言っています。<p/>
  <form action="/users" method="POST">
  <p>name<br>
  <input type="text" name="name"></p>
  <input type="submit" name="commit" value="参加する!!" class="btn btn-danger">
  <input type="hiddne" name="order_id" value="<%= @order.id %>">
</form>
